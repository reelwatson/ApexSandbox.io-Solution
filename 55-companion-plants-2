public Boolean companionPlants(List<String> plants) {
    //code here
    Boolean eachHasCompanion = false;

    for (Integer i = 0; i <= plants.size() - 1; i++) {

        // TO DO: fix System.ListException: List index out of bounds
        /*
        TO DO: System.AssertException: Assertion Failed: No plants means our condition is true: 
        all plants are next to a companion plant if there are no plants. 
        In logic, such statements are called vacuously true. I am sorry for this test case. (lol)
        Just return true if the array has size 0.: Expected: true, Actual: false
        */
        if (plants.size() == 0) {
            eachHasCompanion = true;
        }
        else if (plants[i] == 'onions' && (plants[i+1] == 'lettuce' || plants[i+1] == 'carrots' || plants[i+1] == 'tomatoes')) {
            eachHasCompanion = true;
        }
        else if (plants[i] == 'lettuce' && (plants[i+1] == 'cucumbers' || plants[i+1] == 'onions')) {
            eachHasCompanion = true;
        }
        else if (plants[i] == 'cucumbers' && (plants[i+1] == 'lettuce')) {
            eachHasCompanion = true;
        }
        else if (plants[i+1] == 'onions' && (plants[i] == 'carrots' || plants[i] == 'tomatoes')) {
            eachHasCompanion = true;
        }
    }

    return eachHasCompanion;
}

/*
If the first plant is onions, the second should be lettuce, carrots, or tomatoes
If the first plant is lettuce, then the second should be cucumbers or onions. 
If the first plant is cucumbers, the second should be lettuce. 
If the first plant is carrots, the second should be onions. 
If the first plant is tomatoes, the second should be onions. 
If none of the above match, the plants are not companions.
*/