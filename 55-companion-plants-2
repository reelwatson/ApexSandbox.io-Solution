public Boolean companionPlants(List<String> plants) {
    //code here
    Boolean eachHasCompanion = false;
    // Create variables for first comparison string, second comparison string
    String firstPlant = plants[0;]
    String secondPlant = plants[1];

    for (String plant : plants)


    return eachHasCompanion;
}